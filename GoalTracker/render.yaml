services:
  # ── Backend API (FastAPI) ─────────────────────────────────────
  - type: web
    name: harvest-goaltracker
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3   # newer Pythons (3.13+) have no prebuilt pydantic-core wheel yet, forcing a Rust/maturin
                        # source build that fails on Render's read-only build filesystem
      - key: DATABASE_URL
        sync: false   # set in the Render dashboard - same Supabase Postgres project as AuditApp/CurriculumTracker/TicketTracker/Timetable
      - key: SECRET_KEY
        generateValue: true
      - key: RESEND_API_KEY
        sync: false
      - key: RESEND_FROM_EMAIL
        sync: false
      - key: APP_URL
        value: https://harvest-goaltracker-ui.onrender.com

  # ── Frontend (React / Vite static site) ──────────────────────
  - type: web
    name: harvest-goaltracker-ui
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://harvest-goaltracker.onrender.com   # update after backend deploys
      - key: VITE_PORTAL_URL
        value: https://harvest-portal.onrender.com/portal/login.html
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    headers:
      # index.html carries the baked-in VITE_API_URL/VITE_PORTAL_URL and must
      # always be revalidated - otherwise browsers/CDNs cache the app shell
      # from an older build and every deploy after the first appears to do
      # nothing until the visitor hard-refreshes.
      - path: /
        name: Cache-Control
        value: no-cache, no-store, must-revalidate
      - path: /index.html
        name: Cache-Control
        value: no-cache, no-store, must-revalidate

  # ── Daily flag-check + email nudge ───────────────────────────
  - type: cron
    name: harvest-goaltracker-flagcheck
    runtime: python
    rootDir: backend
    schedule: "0 3 * * *"   # 03:00 UTC daily (~08:30 IST)
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.flag_check
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: RESEND_API_KEY
        sync: false
      - key: RESEND_FROM_EMAIL
        sync: false
      - key: APP_URL
        value: https://harvest-goaltracker-ui.onrender.com
