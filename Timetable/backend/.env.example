# Same Supabase Postgres project as AuditApp/the portal (shared `users` table for auth,
# Timetable's own tables live alongside it).
DATABASE_URL=postgresql://user:password@host:5432/postgres?sslmode=require
SECRET_KEY=change_me_in_prod
